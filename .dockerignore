.git
.gitignore
node_modules
.next
out
build
coverage
.DS_Store

# env files stay on the host — compose passes them via env_file / -e
.env
.env.*
!.env.example

# uploaded media is never baked into the image
media/

# editor / IDE
.vscode
.idea
*.swp

# docs + dev-only
Dockerfile
docker-compose*.yml
.dockerignore
*.md
!README.md

# local caches
.turbo
.cache
.pnpm-store
*.log
